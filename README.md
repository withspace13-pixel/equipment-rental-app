# 프린터&TV 대여 계산기

React + Tailwind(Vite) 기반 대여 견적 계산기입니다.

## 로컬 실행

```bash
npm install
npm run dev
```

브라우저에서 `http://localhost:5173` 접속 후 사용하세요.

## 빌드

```bash
npm run build
```

## 배포

Vercel에서는 해당 리포지토리를 연결하면 `npm run build` 후 `dist`를 자동 서빙합니다.

